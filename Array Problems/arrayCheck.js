let arr = [1, 2, 3, 4];

array(arr);
// const is_array = (array) => {
//   let result = Array.isArray(array);

//   console.log(result);
// };

function array(arr) {
  let result = Array.isArray(arr);

  console.log(result);
}
